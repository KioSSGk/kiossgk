import React from 'react';
import Logout from '../components/admin/Logout';

const LoginPage = () => {
    return (
        <div>
            <Logout />
        </div>
    );
};

export default LoginPage;
